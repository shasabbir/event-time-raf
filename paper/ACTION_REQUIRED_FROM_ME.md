# Action Required From Me

1. Replace `Author Name` in `main.tex` with the final author names, affiliations, institutional email addresses, and corresponding-author details.
2. Rerun notebooks 01 and 02 from a clean environment using the current validation-only M12 selection logic. Preserve the new selection log, predictions, tables, figures, run manifest, and checksums. Until then, keep M12 labeled post hoc and exploratory.
3. Run the locked protocol on a second city, county, or independent air-quality dataset to satisfy the mandatory external-validation requirement. Report the new dataset construction and all metrics separately.
4. Record the exact CPU, GPU, RAM, operating-system image, and peak memory during the clean run so the experimental setup is complete.
5. Verify all prose and bibliography entries against the cited sources, run the institution's originality check, and confirm that the final manuscript complies with authorship and AI-use rules.
6. Decide whether the intended claim is retrospective event-context forecasting or strict real-time forecasting. The latter requires an event source with genuine issue/publication timestamps; NOAA Storm Events alone does not provide them.

## Enrichment Versus Implemented Code

No unimplemented neural module was introduced as though it existed. SACB, LSER, and DFEH are names assigned to behavior already present in the repository and archived outputs. The compact tensor forward pass, terminology, and four diagrams are manuscript enrichments that formalize the implementation; they do not add new model code.

The validation-only drift-gate procedure is present in the current notebook, but the archived M12 numbers were generated by post-hoc selection from test predictions. Any future statement that M12 is validation-selected depends on completing Action 2. No abstract or contribution claim was strengthened beyond the available evidence; unsupported superiority language was removed.

