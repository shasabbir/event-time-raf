\section{Methodology}

\subsection{Overview}
Fig.~\ref{fig:architecture} shows the Event-TimeRAF pipeline. The first step is to align air-quality observations with weather, calendar, and availability-filtered event features. The system then creates sliding windows and a time-series knowledge base. The implemented hybrid retriever ranks historical windows using PM2.5, weather, calendar, and event-context similarity. A separate source-record lookup links recent event identifiers to explanations; it does not supply future event labels to the model. The retrieved evidence is combined with structured features to generate a forecast, uncertainty proxy, drift score, and explanation.

For each forecast origin $t$, Event-TimeRAF maps the aligned context into a
forecast through
\begin{equation}
    \hat{Y}_{t+1:t+H} =
    F_{\theta}\bigl(\phi(X_t,W_t,C_t,E_t), G_t, D_t\bigr),
    \label{eq:event_timeraf_mapping}
\end{equation}
where $\phi(\cdot)$ denotes the tabular feature construction, $G_t$ denotes
retrieved historical evidence, and $D_t$ denotes drift evidence. The key
methodological constraint is that every element of $G_t$ and $D_t$ must be
available no later than the forecast origin.

\begin{figure}[!t]
\centering
\resizebox{\columnwidth}{!}{%
\begin{tikzpicture}[
    node distance=4.5mm,
    block/.style={rectangle, rounded corners, draw=black, align=center, minimum width=35mm, minimum height=6mm, font=\scriptsize},
    smallblock/.style={rectangle, rounded corners, draw=black, align=center, minimum width=28mm, minimum height=6mm, font=\scriptsize},
    arrow/.style={-Latex, thick}
]
\node[block] (air) {Historical PM2.5\\Data};
\node[block, below=of air] (align) {Weather + Calendar\\Alignment};
\node[block, below=of align] (windows) {Sliding Window\\Construction};
\node[smallblock, below left=7mm and 8mm of windows] (tskb) {Time-Series\\Knowledge Base};
\node[smallblock, below right=7mm and 8mm of windows] (evkb) {Event\\Knowledge Base};
\node[smallblock, below=of tskb] (tsret) {Time-Series\\Retriever};
\node[smallblock, below=of evkb] (evret) {Event\\Retriever};
\node[block, below right=8mm and -15mm of tsret] (fusion) {Hybrid Fusion\\Module};
\node[block, below=of fusion] (model) {Forecasting\\Backbone};
\node[block, below=of model] (out) {Forecast + Confidence\\+ Drift + Explanation};

\draw[arrow] (air) -- (align);
\draw[arrow] (align) -- (windows);
\draw[arrow] (windows) -- (tskb);
\draw[arrow] (windows) -- (evkb);
\draw[arrow] (tskb) -- (tsret);
\draw[arrow] (evkb) -- (evret);
\draw[arrow] (windows) -- (fusion);
\draw[arrow] (tsret) -- (fusion);
\draw[arrow] (evret) -- (fusion);
\draw[arrow] (fusion) -- (model);
\draw[arrow] (model) -- (out);
\end{tikzpicture}
}
\caption{Event-TimeRAF forecasting architecture.}
\label{fig:architecture}
\end{figure}

\subsection{Relationship to TimeRAF}
TimeRAF retrieves top-$k$ candidate time-series windows from a curated knowledge
base and incorporates the retrieved candidates using Channel Prompting
\cite{zhang2025timeraf}. It uses a learnable MLP-based dual-encoder retriever,
dot-product similarity, a frozen TSFM backbone, and candidate augmentation
during retriever training. Event-TimeRAF keeps the retrieval-augmentation
principle, but changes the application setting from general zero-shot benchmark
forecasting to official-source Los Angeles County PM2.5 forecasting with
weather, calendar, event, drift, and explanation constraints.

Table~\ref{tab:timeraf_comparison} gives the direct methodological comparison.
The comparison is not a head-to-head performance ranking because the two studies
use different datasets, forecast horizons, and evaluation protocols. The key
distinction is that TimeRAF is a full neural retrieval-augmented TSFM
architecture, whereas Event-TimeRAF is a domain adaptation that prioritizes
official data provenance, temporally leakage-safe retrieval, event-availability
auditing, and evidence-grounded environmental explanations. A dual-encoder
retriever, candidate augmentation, and full internal Channel Prompting are
therefore treated as future extensions rather than completed components in this
implementation.

\subsection{Dataset Construction}
Hourly PM2.5 observations are taken from US EPA AirData/AQS parameter 88101 for Los Angeles County \cite{epaairdata2026}. The data audit selects one monitoring site using coverage and continuity criteria when a single site satisfies the coverage criterion. Because no individual monitor met this criterion, the target is the hourly county median across official Los Angeles County AQS monitors. Timestamps are converted to \texttt{America/Los\_Angeles} with explicit daylight-saving handling. Short input gaps may be filled only from past-available observations; targets are never interpolated. Weather covariates come from the nearest suitable NOAA Integrated Surface Database station \cite{noaaisd2026}. Calendar features include hour, weekday, weekend, month, season, US federal holidays, and California holidays.

All normalization statistics are fitted on the training split only. The same normalization procedure is used for query windows and retrieved candidate windows, following the TimeRAF requirement that input and retrieved sequences use consistent preprocessing.

\subsection{Time-Series Knowledge Base}
The time-series knowledge base contains historical PM2.5 windows. Each record stores a window identifier, start time, end time, input window values, future window values, weather summary, calendar summary, normalized window vector, data split, and normalization identifier. Knowledge-base origins are separated by 192 hours, equal to the 168-hour input and 24-hour future combined. For every query, a candidate is eligible only when its complete future ends before the query input begins. Validation and test retrieval is additionally restricted to training-history windows. These rules exclude self-matches, near-duplicate windows, and overlap with the query context.

Formally, the knowledge base is
\begin{equation}
    \mathcal{K} = \{(u_i, X_i, Y_i, m_i, c_i, e_i)\}_{i=1}^{N},
\end{equation}
where $u_i$ is the candidate origin, $X_i$ and $Y_i$ are the candidate lookback
and future windows, $m_i$ is a weather summary, $c_i$ is a calendar summary, and
$e_i$ is the event-context summary available at $u_i$. For a query origin $t$,
    the temporally eligible set is
\begin{equation}
    \mathcal{A}_t =
    \{i: u_i + H < t-L+1,\; i \in \mathcal{T}_{train}\},
    \label{eq:eligible_set}
\end{equation}
for validation and test queries. The strict inequality in
(\ref{eq:eligible_set}) ensures that a candidate's target period finishes before
the query input period begins.

\begin{figure}[!t]
\centering
\resizebox{\columnwidth}{!}{%
\begin{tikzpicture}[
    axis/.style={-Latex, thick},
    span/.style={rectangle, draw=black, minimum height=6mm, align=center, font=\scriptsize},
    tlabel/.style={font=\scriptsize},
    brace/.style={-Latex, thick}
]
\draw[axis] (0,0) -- (11,0);
\node[tlabel, below] at (0,0) {past};
\node[tlabel, below] at (11,0) {future};

\node[span, fill=gray!10, minimum width=2.7cm] (cin) at (2.0,0.55) {candidate\\input};
\node[span, fill=gray!25, minimum width=1.2cm, right=0mm of cin] (ctar) {candidate\\target};
\node[tlabel, above] at (3.95,1.0) {$u_i+H$};

\node[span, fill=blue!8, minimum width=2.7cm] (qin) at (7.4,0.55) {query\\input};
\node[span, fill=blue!22, minimum width=1.2cm, right=0mm of qin] (qtar) {query\\target};
\node[tlabel, above] at (6.05,1.0) {$t-L+1$};
\node[tlabel, above] at (8.75,1.0) {$t$};

\draw[dashed] (4.0,-0.35) -- (4.0,1.15);
\draw[dashed] (6.05,-0.35) -- (6.05,1.15);
\draw[brace] (4.15,-0.65) -- node[below, font=\scriptsize] {required gap} (5.9,-0.65);
\node[tlabel, align=center] at (5.05,-1.25) {eligible only when the\\candidate target ends first};
\end{tikzpicture}
}
\caption{Leakage-safe retrieval timeline used to enforce temporal eligibility.}
\label{fig:retrieval_timeline}
\end{figure}

Given a query window $X_t$, the time-series retriever returns
\begin{equation}
    R^{ts}_t = \{r^{ts}_{t,1}, r^{ts}_{t,2}, \ldots, r^{ts}_{t,k}\},
\end{equation}
where $k$ is the number of retrieved candidates. The default setting is $k=8$, with sensitivity analysis for $k \in \{1,4,8,16\}$.

\subsection{Event Knowledge Base}
The event knowledge base contains source-preserving records relevant to Los Angeles County. NOAA Storm Events is the required structured source, while NOAA Hazard Mapping System fire and smoke records remain an optional enrichment \cite{noaastormevents2026,noaahms2026}. Stored fields include event identifier, event start and end, information-availability assumption, location, category, source, source URL, title, and summary. Event categories include wildfire, smoke, excessive heat, high wind, heavy rain, flood, dust, and other weather-related records.

Model inputs represent events with hourly publication-assumption counts, rolling 24-hour, 72-hour, and 168-hour counts, category-specific 72-hour counts, active-event counts, and an event-burst ratio. Recent source event identifiers are retained only as explanation evidence. Every event used as a model input must be available by the forecast origin under a recorded availability rule. Evaluation distinguishes events published in the prior 72 hours, events active at the origin, and recorded event intervals overlapping the target horizon. Target overlap is used only as a post-hoc label and never as a model input. NOAA Storm Events does not provide a machine-readable publication timestamp; consequently, the implementation records event start as a retrospective availability assumption. Results that use this assumption are labeled sensitivity experiments rather than strict operational forecasts. Strict experiments require source records with genuine publication or issuance timestamps.

\subsection{Hybrid Retrieval Score}
The hybrid retrieval score combines time-series similarity, weather relevance, calendar relevance, and event relevance:
\begin{equation}
    s(q, i) =
    \alpha s_{ts}(q,i) +
    \beta s_{w}(q,i) +
    \gamma s_{c}(q,i) +
    \delta s_{e}(q,i),
    \label{eq:hybrid_score}
\end{equation}
where $s_{ts}$ is time-series similarity, $s_w$ is weather similarity, $s_c$ is calendar similarity, and $s_e$ is event relevance. The implemented weights are $\alpha=0.5$, $\beta=0.2$, $\gamma=0.1$, and $\delta=0.2$.

The retrieved set is selected as
\begin{equation}
    G_t = \operatorname{TopK}_{i \in \mathcal{A}_t}\; s(t,i).
    \label{eq:topk}
\end{equation}
For retrieval-only baselines, the forecast is the uniform or
similarity-weighted average of the retrieved candidates' future PM2.5 windows.
For feature-level models, the retrieved futures and candidate metadata are
summarized as mean, spread, similarity, and event-match features.

\subsection{Fusion and Forecasting Backbone}
The feature-level fusion method combines PM2.5 lag/rolling features, weather
features, calendar features, retrieved time-series summary features, event
features, and drift indicators. The tabular forecasting backbone uses 24 direct
XGBoost regressors, one for each forecast horizon, providing an efficient and
competitive supervised baseline. Frozen Chronos-Bolt is evaluated separately,
both without retrieval and with validation-weighted forecast-level fusion
between Chronos and retrieved historical forecasts. Unlike TimeRAF, the
evaluated architecture does not modify the foundation model's internal
representation through Channel Prompting.

The tabular fusion stage includes a lightweight drift-conditioned selector. It compares
the strongest validation candidates separately on drift and non-drift validation
origins, then applies the selected source to the corresponding test origins:
\begin{equation}
    \hat{Y}^{G}_t =
    \begin{cases}
    \hat{Y}^{a}_{t}, & D_t = 0,\\
    \hat{Y}^{b}_{t}, & D_t = 1,
    \end{cases}
    \label{eq:drift_gate}
\end{equation}
where $a$ and $b$ are chosen only from validation errors. This gate does not
train a new model; it tests whether a transparent drift-specific selection rule
can avoid applying the same forecaster in all regimes.

The Chronos-Bolt retrieval variant uses forecast-level fusion rather than internal
Channel Prompting. Let $\hat{Y}^{C}_t$ be the frozen Chronos forecast and
$\hat{Y}^{R}_t$ be the retrieved historical forecast. The fused prediction is
\begin{equation}
    \hat{Y}^{CR}_t(\omega) =
    \omega \hat{Y}^{C}_t + (1-\omega)\hat{Y}^{R}_t,
    \label{eq:chronos_fusion}
\end{equation}
where $\omega \in \{0,0.25,0.5,0.75,1.0\}$ is selected on the validation split.
Validation selected $\omega=0.75$.

\subsection{Concept Drift Detection}
The implementation represents operational distribution shift using transparent indicators rather than claiming perfect concept-drift identification. The indicators are recent mean shift, recent variance shift, retrieval similarity drop, weather-pattern shift, and event-burst ratio. A simple drift score can be defined as
\begin{equation}
    D_t =
    \lambda_1 z_{\mu,t} +
    \lambda_2 z_{\sigma,t} +
    \lambda_3 (1-\bar{s}_{ts,t}) +
    \lambda_4 z_{w,t} +
    \lambda_5 b_{event,t},
\end{equation}
where $z_{\mu,t}$ and $z_{\sigma,t}$ are normalized mean and variance shifts, $\bar{s}_{ts,t}$ is average retrieval similarity, $z_{w,t}$ is weather-pattern shift, and $b_{event,t}$ is an event-burst score. The implemented components are robustly scaled against training data and averaged with equal weight; the alert threshold is selected from validation scores.

\subsection{Explanation Generation}
The explanation module generates evidence-based explanations. It uses signed local XGBoost contributions averaged across the 24 direct horizon models, weather flags, retrieved historical windows, retrieved event records, calendar context, forecast direction, component-level drift evidence, and an uncertainty proxy combining retrieved-trajectory spread with validation residual error. It does not generate explanations from an unrestricted language model. Each explanation is traceable to stored machine-readable evidence.

\begin{figure}[!t]
\centering
\resizebox{\columnwidth}{!}{%
\begin{tikzpicture}[
    node distance=4mm,
    block/.style={rectangle, rounded corners, draw=black, align=center, minimum width=28mm, minimum height=6mm, font=\scriptsize},
    evidence/.style={rectangle, draw=black, align=center, minimum width=22mm, minimum height=6mm, font=\scriptsize, fill=gray!10},
    arrow/.style={-Latex, thick}
]
\node[evidence] (features) {feature\\effects};
\node[evidence, below=of features] (retrieved) {retrieved\\windows};
\node[evidence, below=of retrieved] (events) {event\\IDs};
\node[evidence, below=of events] (drift) {drift\\components};
\node[evidence, below=of drift] (uncertainty) {uncertainty\\proxy};
\node[block, right=12mm of events] (record) {machine-readable\\evidence record};
\node[block, right=12mm of record] (text) {forecast\\explanation};

\draw[arrow] (features.east) -- (record.west);
\draw[arrow] (retrieved.east) -- (record.west);
\draw[arrow] (events.east) -- (record.west);
\draw[arrow] (drift.east) -- (record.west);
\draw[arrow] (uncertainty.east) -- (record.west);
\draw[arrow] (record) -- (text);
\end{tikzpicture}
}
\caption{Evidence-grounded explanation flow used by Event-TimeRAF.}
\label{fig:explanation_flow}
\end{figure}

Table~\ref{tab:timeraf_comparison} also clarifies which parts of TimeRAF were
implemented directly and which parts were adapted for the environmental setting.

\begin{table*}[!t]
\centering
\caption{TimeRAF Versus Event-TimeRAF}
\label{tab:timeraf_comparison}
\begin{tabular}{p{0.15\textwidth}p{0.38\textwidth}p{0.39\textwidth}}
\toprule
Aspect & TimeRAF & Event-TimeRAF \\
\midrule
Primary objective & Improve zero-shot time-series forecasting by augmenting a
frozen TSFM with retrieved external time-series knowledge. & Test whether the
TimeRAF retrieval idea helps an official-source, event-sensitive PM2.5
forecasting task under drift and availability constraints. \\
Forecasting domain & Multi-domain time-series benchmarks, with zero-shot
generalization as the main setting. & Los Angeles County hourly PM2.5
forecasting for 2019--2024. \\
Knowledge base & Curated time-series windows built from multi-domain or
domain-specific datasets, normalized consistently with the query. & Hybrid
official-source knowledge base containing PM2.5 windows, NOAA weather summaries,
calendar signatures, and NOAA Storm Events context. \\
Retriever & Learnable MLP-based dual encoder with dot-product scores and
forecaster-guided retriever learning. & Random, cosine, and transparent hybrid
similarity retrievers; no learned dual encoder is used. \\
Candidate count & Uses top-$k$ retrieval; the TimeRAF study uses $k=8$ as the
default and studies candidate-count sensitivity. & Uses $k=8$ as the default
and reports sensitivity for $k \in \{1,4,8,16\}$. \\
Knowledge integration & Channel Prompting integrates retrieved candidates into
the frozen TSFM representation. & Feature-level fusion for XGBoost models,
validation-selected drift gating, and validation-weighted forecast-level fusion
for frozen Chronos-Bolt. \\
Backbone & Frozen TSFM backbone, with TTM-Base reported as the principal
backbone. & Direct XGBoost horizon models and frozen
\texttt{amazon/chronos-bolt-small}. \\
Leakage control & Uses non-overlapping knowledge-base windows and retrieval
constraints, including avoiding same-dataset retrieval during training. & Requires
each candidate's target window to end before the query lookback begins; validation
and test retrieval are restricted to training-history windows. \\
Event handling & Not an explicit component of the base method. & Adds
availability-tagged storm-event features and source-record evidence; event
results are labeled retrospective when strict publication timestamps are absent.
\\
Drift handling & Not a central module in the base method. & Adds explicit mean,
variance, retrieval-similarity, weather, and event-burst drift indicators. \\
Explainability & Provides retrieved-case visualizations to inspect how retrieved
knowledge supports forecasting. & Stores evidence-grounded explanations using
feature effects, retrieved windows, event IDs, drift components, and uncertainty
proxies. \\
Implementation status & Full proposed neural architecture with learnable
retrieval and Channel Prompting. & TimeRAF-inspired domain implementation;
learnable retriever and internal Channel Prompting remain future work. \\
\bottomrule
\end{tabular}
\end{table*}
